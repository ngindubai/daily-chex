{
  "name": "daily-chex",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "render-build": "cd app && npm install && npm run build && cd ../server && npm install && npm run build",
    "render-start": "cd server && node dist/db/migrate.js && node dist/db/seed-safe.js && node dist/index.js",
    "dev": "cd server && npm run dev",
    "seed": "cd server && npm run db:seed"
  },
  "engines": {
    "node": ">=20"
  }
}
